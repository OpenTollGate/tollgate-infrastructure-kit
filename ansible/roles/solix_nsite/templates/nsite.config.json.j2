{
  "$schema": "https://nsyte.run/schemas/config.schema.json",
  "relays": {{ solix_nsite_relays | to_nice_json }},
  "servers": {{ solix_nsite_servers | to_nice_json }},
  "id": null,
  "title": "contextvm-anker-solix",
  "description": "Anker SOLIX C1000 BLE Bridge — ESP32 + Nostr control dashboard — deployed from {{ base_domain }} infrastructure"
}
