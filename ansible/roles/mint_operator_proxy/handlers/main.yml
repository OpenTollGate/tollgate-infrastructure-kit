---
- name: restart mint operator proxy
  systemd:
    name: mint-operator-proxy
    state: restarted
    daemon_reload: yes

- name: reload caddy
  shell: docker exec tollgate-caddy caddy reload --config /etc/caddy/Caddyfile 2>&1
